# Reeris UI 0.52 — Identity Reservation & Publication Clearance

Reeris UI is the selected pre-1.0 identity. The canonical target is `Eristavi/ReerisUI` and the intended npm ecosystem is `@reeris/*`.

## Current result

Public exact/near-exact research remains encouraging: no material exact-match Reeris UI software framework, design system, developer platform, or exact software trademark was surfaced in the research pass. This is **not** the same as a reservation or legal clearance.

The publication naming gate therefore remains **OPEN**.

## npm reservation targets

Reserve the `reeris` npm identity/scope first, then reserve/publish the following package names only from an account that controls that scope:

- `@reeris/core`
- `@reeris/icons`
- `@reeris/js`
- `@reeris/react`
- `@reeris/vue`
- `@reeris/svelte`

Anonymous package-page verification returned HTTP 403 in the research environment. Search indexing did not surface these exact packages, but that is **not proof of availability**. The authoritative action is account-level reservation/creation on npm.

## GitHub target

Canonical repository: `Eristavi/ReerisUI`.

The public URL returned 404 at the time of this check. That may mean the repository is not yet renamed/created, is private, or is not publicly resolvable. The correct account-level action is to rename the existing repository or create `ReerisUI` under the existing `Eristavi` owner and verify the final public URL.

## Domain candidates

Preferred order:

1. `reeris.dev`
2. `reerisui.dev`
3. `reeris.com`
4. `reerisui.com`
5. `reeris.io`
6. `reeris.org`

No material indexed usage or DNS records were observed for these candidates during this pass. **Do not treat any of these as available until a registrar confirms and completes registration.**

## Trademark boundary

No material exact `REERIS UI` software trademark surfaced in the public research pass. Formal similarity clearance is still pending. Before a public commercial launch or trademark filing, obtain a proper search in the relevant jurisdictions, particularly software/downloadable-goods and software-services classes (commonly Nice Classes 9 and 42).

This document is research and release engineering evidence, **not legal advice**.

## Account-level closure checklist

The publication gate can move toward closure only after these external actions are evidenced:

1. `@reeris` npm identity/scope is under the project owner's control.
2. Intended package names are verified/reserved under that scope.
3. `Eristavi/ReerisUI` exists at the intended visibility and ownership.
4. At least one primary Reeris domain is registered to the project owner.
5. Formal trademark/similarity review is completed to the chosen legal standard.

Until then Reeris development may continue, but public package publication remains gated.
